# BIM ビューワの民主化

## 目的

このプロジェクトの目的は、BIM データを専門家だけのものにしないことです。

BIM を社会実装するには、設計者や施工者だけでなく、発注者、行政、地方自治体、審査機関、維持管理者、教育機関、中小企業、個人事務所が、同じデータを安全に確認し、要素単位で指摘し、合意形成できる必要があります。

IFC Review Viewer は、その入口として IFC を表示・レビューできる透明なオープンソースビューアを提供します。

## 基本方針

- IFC を第一級の公開フォーマットとして扱う
- データを外部へ送らず、ブラウザ上で表示・レビューする
- コメントやレビュー結果を JSON で交換できるようにする
- 特定ベンダーや特定クラウドへの依存を避ける
- 動作を検証できるよう、ソースコードとビルド手順を公開する
- 将来の拡張は、コアの安全性と単純さを壊さない形で行う

## 社会的な狙い

まず IFC ビューアとして公開し、誰でも安全に BIM データを確認できることを示します。

そのうえで、将来的には IFC だけでなく、各種 BIM オーサリングツールから変換された表示用データも同じレビュー体験で扱えるようにします。これにより、既存業務で作られた BIM データを、より多くの関係者が確認・レビューできるようになります。

この流れが広がれば、国土交通省を含む行政、地方自治体、各種団体、企業、個人事務所が BIM データを安心して受け取り、確認し、活用する土台になります。

## 安全性への考え方

安全性は「安全だと宣言すること」ではなく、「検証できること」によって得られます。

このプロジェクトでは、次の点を重視します。

- ローカルファイルをブラウザ内で処理する
- 不要な通信を行わない
- 保存形式を読みやすい JSON にする
- 依存ライブラリとライセンスを明示する
- 変更履歴、レビュー、検証手順を公開できる形にする

## 拡張の考え方

将来の拡張は、コアビューアと分離します。

コアビューアは、IFC 表示、要素選択、属性表示、コメント記録という最小限の信頼できる機能を守ります。追加の変換データ表示、レビュー形式、連携機能は、アダプターやオプション機能として扱います。

これにより、利用者は必要な機能だけを選び、行政・団体・企業でも導入判断しやすい構成にできます。
