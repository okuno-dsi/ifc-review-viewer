# ST-Bridge Model Folder

Place approved ST-Bridge `.stb` or `.xml` files here when a structural review package needs them.

The viewer cannot automatically scan this folder from a local browser session. Open the viewer, choose `ST-Bridge を追加`, and select the files manually.

Do not commit project model files unless they are explicitly approved public sample data.
